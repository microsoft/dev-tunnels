{
	"name": "@microsoft/dev-tunnels-test",
	"description": "Tests for the tunnels library for Visual Studio tools",
	"keywords": [
		"Tunnels"
	],
	"scripts": {
		"compile": "npm run -C ../.. compile",
		"watch": "npm run -C ../.. watch .",
		"test": "npm run -C ../.. test"
	}
}
